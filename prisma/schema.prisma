generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

//Student Table
model Student {
  id                String             @id @default(cuid())
  name              String
  dateOfBirth       String
  aadharNumber      String             @unique
}

